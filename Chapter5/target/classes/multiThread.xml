<?xml version="1.0" encoding="UTF-8" ?>
<suite name = "Thread" parallel="classs" thread-count="3">

    <!--
    tests级别：不同的test、tag下的用例可以在不同的线程下执行
                相同的test、tag下的用例只能在同一个线程中执行
    classs级别：不同class、tag下的用例在同一个线程中执行
                不同的class、tag下的用例可以在不同的线程中执行
    methods级别：所有用例都可以再不同的线程下执行
    thread-count：代表了最大的并发线程数
    xml文件配置这种方式不能指定线程池，只有方法上才可以指定线程池
    -->
    <test name = "demo1">
        <classes name = "d1">
            <class name = "com.course.testng.multiThread.MultiThreadOnXml"/>
            <class name="com.course.testng.basicAnnotaion"/>
            <class name = "com.course.testng.multiThread.MultiThreadOnXml"/>
        </classes>
        <classes name = "d2">
            <class name = "com.course.testng.multiThread.MultiThreadOnXml"/>
            <class name="com.course.testng.basicAnnotaion"/>
            <class name = "com.course.testng.multiThread.MultiThreadOnXml"/>
        </classes>
    </test>

    <test name = "demo2">
        <classes name = "d3">
            <class name="com.course.testng.basicAnnotaion"/>
        </classes>
    </test>
</suite>